# mailpush server (FastAPI;收读走 imaplib 连接池,发信走 smtplib)
fastapi>=0.115,<0.116
uvicorn[standard]>=0.32,<0.35
firebase-admin>=6.5,<7
pydantic>=2.7,<3
email-validator>=2,<3       # models.py 的 EmailStr 需要它,否则 mail-api 启动即崩
python-multipart>=0.0.9     # /send 的 multipart(Form/File/UploadFile)需要它
msal>=1.28,<2               # OAuth2(Outlook 等)设备码授权 + 静默刷新(oauth.py)
pyyaml>=6,<7                # render_imapnotify.py 解析手写 base + 生成 goimapnotify 配置
